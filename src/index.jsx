/* eslint-disable import/no-extraneous-dependencies */
import { render } from 'react-dom';
import Authentication from './components/Authentication';

const root = document.getElementById('root');

render((<Authentication />), root);
